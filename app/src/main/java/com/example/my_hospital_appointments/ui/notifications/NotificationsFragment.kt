package com.example.my_hospital_appointments.ui.notifications

import android.view.View
import androidx.fragment.app.Fragment
//import com.example.my_hospital_appointments.databinding.FragmentNotificationsBinding

class NotificationsFragment : Fragment() {
    /*
    private var binding: FragmentNotificationsBinding? = null
    override fun onCreateView(
        inflater: LayoutInflater,
        container: ViewGroup?, savedInstanceState: Bundle?
    ): View? {
        val notificationsViewModel: NotificationsViewModel =
            ViewModelProvider(this).get<NotificationsViewModel>(
                NotificationsViewModel::class.java
            )
        binding = FragmentNotificationsBinding.inflate(inflater, container, false)
        val root: View = binding.getRoot()
        val textView: TextView = binding.textNotifications
        notificationsViewModel.text.observe(viewLifecycleOwner) { text: String? ->
            textView.setText(
                text
            ) }
        return root
    }

    override fun onDestroyView() {
        super.onDestroyView()
        binding = null
    }

     */
}